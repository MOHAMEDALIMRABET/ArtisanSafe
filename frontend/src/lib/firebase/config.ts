/**
 * Configuration Firebase
 * Réexporte depuis lib/firebase.ts pour compatibilité avec la structure firebase/
 */

export { auth, db, storage } from '../firebase';
export { default as app } from '../firebase';
