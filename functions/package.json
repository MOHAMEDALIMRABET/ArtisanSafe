{
  "name": "artisandispo-functions",
  "version": "1.0.0",
  "description": "Cloud Functions pour ArtisanSafe - Annulation automatique devis non payés",
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "npm run build && firebase deploy --only functions",
    "deploy:annulation": "npm run build && firebase deploy --only functions:annulerDevisNonPayes",
    "logs": "firebase functions:log",
    "logs:annulation": "firebase functions:log --only annulerDevisNonPayes"
  },
  "private": true
}
